# need to make scripts to run on each aws instance
# s0rs0 -> s0rs2 + s1rs0 -> ... + confrs0 -> confrs2 + mongos
# spin up the mongos instances, then initiate the replica sets, then add the shards and enable sharding
# maybe also have one for website
