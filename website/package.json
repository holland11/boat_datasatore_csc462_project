{
  "name": "mongodb_webapp",
  "version": "1.0.0",
  "description": "web app to let clients make queries to our mongodb cluster",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.2.7"
  }
}
